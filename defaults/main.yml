# Standards: 0.1
---
# @var virtualbox_user:description: User to run user-specific commands
# @var virtualbox_user:value: virtualbox_user | default(homebrew_user) | default(ansible_user_id)
# virtualbox_user:

virtualbox_machines: []

...
